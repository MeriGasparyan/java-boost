I tried to solve this problem via introducing a bit of 
polymorphism but I am unsure whether we should include the exception throw as all, 
because in this design one cannot exactly create an invalid employee type

Also, not that I wrote some tests for checking the logic here